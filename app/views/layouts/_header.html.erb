<%= nav_bar fixed: "top", inverse: false, brand: glyph("home"), brand_link: root_path, responsive: true do %>
	<%= menu_group do %>
				<%= menu_item "Shopping", list_items_path, class: "visible-lg" %>
				<%=menu_item "Shop", list_items_path, class: "visible-sm"%>
				<%= menu_item "Tasks", tasks_path, class: "visible-lg" %>
				<%= menu_item "Task", tasks_path, class: "visible-sm" %>
				<%= menu_item "Reports", reports_path, class: "visible-lg" %>
				<%= menu_item "Reports", reports_path, class: "visible-sm" %>
				<%#= menu_item glyph("shopping-cart"), list_items_path %>
				<%#= menu_item glyph("list"), tasks_path %>
				<%#= menu_item glyph("calendar"), last30days_path %>
				<% if user_signed_in? && current_user.admin && current_user.approved %>
					<%= drop_down "NDB" do %>
						<%= menu_item "index", ndb_index_path %>
					 <% end %> <!--drop_down -->
				<% end %><!--if-->
	<% end %> <!-- menu_group -->
		<% if user_signed_in? %>
	<%= menu_group pull: "right" do %>
			<%#= drop_down glyph("user") do %>
				<%= menu_item "Profile", edit_user_registration_path %>
			 <%# end %> <!--drop_down -->
		<%# else %>
			<%#= menu_item glyph("user"), new_user_session_path %>		
		<% end %> <!--if -->
	<% end %> <!--menu_group -->
<% end %> <!--nav_bar-->

