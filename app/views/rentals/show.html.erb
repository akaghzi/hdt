<h3 class="text-muted">
	<%= @rental.task.name.titleize %>:
</h3>
<dl class="dl-horizontal">
	<dt>Name</dt>
	<dd><%= @rental.name.titleize %></dd>
	<dt>Store</dt>
	<dd><%= @rental.store.name.titleize %></dd>
	<dt>Total Price</dt>
	<dd><%= @rental.price %></dd>
</dl>
<%= link_to 'Back', task_path(@rental.task_id) , class: "btn btn-warning btn-sm" %>
