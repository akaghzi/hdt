<div class="form-horizontal" role="form">
<%= form_for(@list_type) do |f| %>
  <% if @list_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list_type.errors.count, "error") %> prohibited this list_type from being saved:</h2>

      <ul>
      <% @list_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="form-group">
		<div class="col-sm-2 form-label">
			<%= f.label :name %>
		</div>
		<div class="col-sm-4">
			<%= f.text_field :name, class: "form-control" %>
		</div>
	</div>
	<div class="form-group">
		<div class="col-sm-2 form-label">
			<%= f.label :description %>
		</div>
		<div class="col-sm-4">
			<%= f.text_field :description, class: "form-control" %>
		</div>
	</div>
	<div class="form-group">
		<div class="col-sm-offset-2 col-sm-10">
			<%= f.submit "submit", class: "btn btn-primary" %>
		</div>
	</div>
	<% end %>	
</div>